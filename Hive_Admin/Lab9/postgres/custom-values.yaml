tls:
  enabled: true
  autoGenerated: true
ldap:
  enabled: true
  server: "openldap.default.svc.cluster.local"
  port: 389
  basedn: "dc=min,dc=io"
  binddn: "cn=admin,dc=min,dc=io"
  bindpw: "admin"
  searchFilter: "(uid=%s)"
  groupSearchBaseDn: "ou=sixgroups,dc=min,dc=io"
  groupSearchFilter: "(&(objectclass=groupOfNames)(member=%d))"
  scheme: "ldap"


